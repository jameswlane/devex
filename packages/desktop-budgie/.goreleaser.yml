version: 2
project_name: devex-plugin-desktop-budgie
monorepo:
  tag_prefix: plugin-desktop-budgie-
  dir: packages/desktop-budgie
builds:
  - id: plugin-desktop-budgie
    main: .
    binary: devex-plugin-desktop-budgie
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - "-s -w"
      - "-X main.version={{.Version}}"
      - "-X main.commit={{.Commit}}"
      - "-X main.date={{.Date}}"
archives:
  - id: plugin-desktop-budgie-archive
    name_template: "devex-plugin-desktop-budgie_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
checksum:
  name_template: checksums.txt
changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
  groups:
    - title: New Features
      regexp: ^.*?feat(\([[:word:]]+\))??!?:.+$
      order: 0
    - title: Bug Fixes
      regexp: ^.*?fix(\([[:word:]]+\))??!?:.+$
      order: 1
    - title: Others
      order: 999
release:
  github:
    owner: jameswlane
    name: devex
  name_template: "devex-plugin-desktop-budgie v{{ .Version }}"
  mode: replace
  draft: false
  prerelease: auto
