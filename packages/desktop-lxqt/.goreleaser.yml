# yaml-language-server: $schema=https://goreleaser.com/static/schema-pro.json
version: 2
project_name: devex-plugin-desktop-lxqt
monorepo:
  tag_prefix: packages/desktop-lxqt/v
  dir: packages/desktop-lxqt
builds:
  - id: plugin-desktop-lxqt
    main: .
    binary: devex-plugin-desktop-lxqt
    dir: packages/desktop-lxqt
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - "-s -w"
      - "-X main.version={{.Version}}"
      - "-X main.commit={{.Commit}}"
      - "-X main.date={{.Date}}"
archives:
  - id: plugin-desktop-lxqt-archive
    name_template: "devex-plugin-desktop-lxqt_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
checksum:
  name_template: checksums.txt
changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
  groups:
    - title: New Features
      regexp: ^.*?feat(\([[:word:]]+\))??!?:.+$
      order: 0
    - title: Bug Fixes
      regexp: ^.*?fix(\([[:word:]]+\))??!?:.+$
      order: 1
    - title: Others
      order: 999
release:
  github:
    owner: jameswlane
    name: devex
  name_template: "devex-plugin-desktop-lxqt v{{ .Version }}"
  mode: replace
  draft: false
  prerelease: auto
