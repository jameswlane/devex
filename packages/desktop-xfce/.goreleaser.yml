version: 2
project_name: devex-plugin-desktop-xfce
monorepo:
  tag_prefix: plugin-desktop-xfce-
  dir: packages/desktop-xfce
builds:
  - id: plugin-desktop-xfce
    main: .
    binary: devex-plugin-desktop-xfce
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - "-s -w"
      - "-X main.version={{.Version}}"
      - "-X main.commit={{.Commit}}"
      - "-X main.date={{.Date}}"
archives:
  - id: plugin-desktop-xfce-archive
    name_template: "devex-plugin-desktop-xfce_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
checksum:
  name_template: checksums.txt
changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
  groups:
    - title: New Features
      regexp: ^.*?feat(\([[:word:]]+\))??!?:.+$
      order: 0
    - title: Bug Fixes
      regexp: ^.*?fix(\([[:word:]]+\))??!?:.+$
      order: 1
    - title: Others
      order: 999
release:
  github:
    owner: jameswlane
    name: devex
  name_template: "devex-plugin-desktop-xfce v{{ .Version }}"
  mode: replace
  draft: false
  prerelease: auto
