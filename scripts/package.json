{
	"name": "devex-scripts",
	"version": "1.0.0",
	"description": "DevEx build and release scripts",
	"scripts": {
		"generate-registry": "node generate-registry.js"
	},
	"dependencies": {
		"@octokit/rest": "^22.0.0"
	}
}
